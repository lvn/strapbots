# botstrap-points

Super stupid point system for things.

Usage:

```javascript
bot.match()
```

or

```json
{
  "type": "match",
  "require": "botstrap-points",
  "regex": {
    "pattern": "(\\d+) points (from|to) (.*)"
  }
}

```
