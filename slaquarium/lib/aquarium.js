

var Aquarium = function Aquarium(width, height) {
  this.width = width;
  this.height = height;
  this.fish = {};
};

Aquarium.prototype.render = function render() {
  var result = [];

  // draw base
  // result.push([])
  //
  // for (int x = -1; x < this.width + 1; x++) {
  //   for (int y = -1; y < this.height + 1; y++) {
  //     if (x === 0 || x === this.width || y === 0 || y === this.height)
  //   }
  // }
  //
  // return

}

module.exports = Aquarium;
